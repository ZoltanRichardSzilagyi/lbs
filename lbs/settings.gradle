rootProject.name = 'lbs'

rootProject.projectDir.listFiles().each({File file ->
	if(file.isDirectory() && file.getName().startsWith("lbs-")){
		println "Include subproject $file.name"
		include(":$file.name")
	}
		
})
